# Wave-10 integration harness

This directory owns bootstrap boundary checks and later integration composition only. Specialist behavior belongs exclusively to the seven declared lane path sets.

Run `python3 integration/phase25-wave10/verify_boundaries.py` during bootstrap. The complete `make verify-wave-10` gate becomes runnable after specialist packages and the single journey are integrated.
