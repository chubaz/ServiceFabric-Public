# Claude Code Compatibility

Read `AGENTS.md` and `docs/workplans/current.md`; they are authoritative. Run `make agent-preflight` before edits and `make verify-current` before completion.

Claude-specific caution: preserve the Flask `ChoiceLoader` chain when a scoped milestone touches template discovery. Legacy catalogue manifests are advisory inputs and must not override canonical contracts or the current workplan.
