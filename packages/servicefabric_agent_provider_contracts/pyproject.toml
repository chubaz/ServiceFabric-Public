[build-system]
requires = ["setuptools==80.9.0"]
build-backend = "setuptools.build_meta"

[project]
name = "servicefabric-agent-provider-contracts"
version = "0.8.0a1"
description = "Immutable provider execution contracts for ServiceFabric coding agents."
requires-python = ">=3.11,<3.12"
dependencies = [
  "pydantic>=2.13,<3",
  "servicefabric-agentic-contracts==0.7.0a1",
  "servicefabric-contracts==0.4.0a1",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
