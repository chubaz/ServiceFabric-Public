# Wave-4 Verification

The focused gate covers harness preflight, operation and capability model tests, registry safety and idempotency tests, explicit Research Notes authoring, deterministic serialization, CLI composition, and the fresh-workspace acceptance journey. It must also assert that no invocation, MCP, REST, Python projection, route inference, or runtime availability is introduced.

Required commands are `make verify-wave-04`, `make verify-current`, and `git diff --check`.
