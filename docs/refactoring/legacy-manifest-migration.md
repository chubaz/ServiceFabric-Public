# Legacy Manifest Migration

The migration workflow is: inventory, assess, supply explicit context, translate, human review, then future authoring/publication. C1-03 does not rewrite manifests, publish artifacts, create registry records or deploy packages.

Every managed artifact must already have immutable identity. Shared-host removal, composite splitting, tool authoring, Django authoring drafts and registry publication remain later work.
