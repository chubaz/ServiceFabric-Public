[build-system]
requires = ["setuptools==80.9.0"]
build-backend = "setuptools.build_meta"

[project]
name = "servicefabric-example-text-utility"
version = "1.0.0"
requires-python = ">=3.11,<3.12"
dependencies = ["fastapi==0.139.0", "uvicorn==0.51.0"]
