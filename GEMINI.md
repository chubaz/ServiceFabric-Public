# Gemini CLI Compatibility

Read `AGENTS.md` and `docs/workplans/current.md`; they are authoritative. Run `make agent-preflight` before edits and `make verify-current` before completion.

Gemini-specific caution: repository `fabric-manifest.json` files are legacy advisory metadata. Preserve them, but do not treat their prose, internal URLs, or paths as canonical authority.
